// Code generated by schemagen; DO NOT EDIT.
// Edit the struct and rerun: go generate ./...

package derived

import (
	"net/url"
	"time"

	"github.com/mbauer83/effect-golang-web/schema"
)

// RecordSchema describes Record. It is generated from the struct.
var RecordSchema = schema.Documented("Record is one of everything.",
	schema.Struct[Record]("Record",
		schema.DocumentedField("Text is a string.",
			schema.FieldOf("text", schema.Text(),
				func(value Record) string { return value.Text },
				func(value *Record, field string) { value.Text = field })),
		schema.FieldOf("whole", schema.Int(),
			func(value Record) int { return value.Whole },
			func(value *Record, field int) { value.Whole = field }),
		schema.FieldOf("wide", schema.Int64(),
			func(value Record) int64 { return value.Wide },
			func(value *Record, field int64) { value.Wide = field }),
		schema.FieldOf("fraction", schema.Float64(),
			func(value Record) float64 { return value.Fraction },
			func(value *Record, field float64) { value.Fraction = field }),
		schema.FieldOf("flag", schema.Bool(),
			func(value Record) bool { return value.Flag },
			func(value *Record, field bool) { value.Flag = field }),
		schema.FieldOf("opaque", schema.Bytes(),
			func(value Record) []byte { return value.Opaque },
			func(value *Record, field []byte) { value.Opaque = field }),
		schema.FieldOf("moment", schema.Time(),
			func(value Record) time.Time { return value.Moment },
			func(value *Record, field time.Time) { value.Moment = field }),
		schema.FieldOf("names", schema.List(schema.Text()),
			func(value Record) []string { return value.Names },
			func(value *Record, field []string) { value.Names = field }),
		schema.FieldOf("labels", schema.Map(schema.Text()),
			func(value Record) map[string]string { return value.Labels },
			func(value *Record, field map[string]string) { value.Labels = field }),
		schema.FieldOf("maybe", schema.Nullable(schema.Text()),
			func(value Record) *string { return value.Maybe },
			func(value *Record, field *string) { value.Maybe = field }),
		schema.OptionalFieldOf("absent", schema.Int(),
			func(value Record) (int, bool) {
				var absent int
				if value.Absent == nil {
					return absent, false
				}
				return *value.Absent, true
			},
			func(value *Record, field int) { value.Absent = &field }),
		schema.FieldOf("nested", InnerSchema,
			func(value Record) Inner { return value.Nested },
			func(value *Record, field Inner) { value.Nested = field }),
		schema.FieldOf("deep", schema.List(InnerSchema),
			func(value Record) []Inner { return value.Deep },
			func(value *Record, field []Inner) { value.Deep = field }),
		schema.FieldOf("address", addressSchema,
			func(value Record) url.URL { return value.Address },
			func(value *Record, field url.URL) { value.Address = field }),
		schema.DocumentedField("Bounded carries what a struct field cannot: which numbers are admitted.",
			schema.FieldOf("bounded", schema.AtMost(schema.AtLeast(schema.Int(), 1), 10),
				func(value Record) int { return value.Bounded },
				func(value *Record, field int) { value.Bounded = field })),
		schema.DocumentedField("Excluding uses the exclusive bounds.",
			schema.FieldOf("excluding", schema.Below(schema.Above(schema.Float64(), 0), 1),
				func(value Record) float64 { return value.Excluding },
				func(value *Record, field float64) { value.Excluding = field })),
		schema.DocumentedField("Sized is constrained by length, and by a pattern carrying a comma of its own inside braces -- which is why items are not split naively.",
			schema.FieldOf("sized", schema.Matching(schema.MaxLength(schema.MinLength(schema.Text(), 2), 8), "^[a-z]{2,8}$"),
				func(value Record) string { return value.Sized },
				func(value *Record, field string) { value.Sized = field })),
		schema.DocumentedField("Shaped carries a format, which replaces the shape a string would have.",
			schema.FieldOf("shaped", schema.Formatted("uuid"),
				func(value Record) string { return value.Shaped },
				func(value *Record, field string) { value.Shaped = field })),
		schema.DocumentedField("Counted is constrained by how many elements it may carry.",
			schema.FieldOf("counted", schema.MaxItems(schema.MinItems(schema.List(schema.Text()), 1), 4),
				func(value Record) []string { return value.Counted },
				func(value *Record, field []string) { value.Counted = field })),
	))

// InnerSchema describes Inner. It is generated from the struct.
var InnerSchema = schema.Documented("Inner is referred to by name, so its own schema is used.",
	schema.Struct[Inner]("Inner",
		schema.FieldOf("label", schema.Text(),
			func(value Inner) string { return value.Label },
			func(value *Inner, field string) { value.Label = field }),
	))

// UntaggedSchema describes Untagged. It is generated from the struct.
var UntaggedSchema = schema.Documented("Untagged takes its wire names from its fields.",
	schema.Struct[Untagged]("Untagged",
		schema.FieldOf("firstName", schema.Text(),
			func(value Untagged) string { return value.FirstName },
			func(value *Untagged, field string) { value.FirstName = field }),
		schema.FieldOf("age", schema.Int(),
			func(value Untagged) int { return value.Age },
			func(value *Untagged, field int) { value.Age = field }),
	))
